const obj = {
  name: "Kings",
  relationship: "single",
};

const LocalStorage = {
  setItem(product) {
    // localStorage.setItem("product", JSON.stringify(product));
  },

  getItem() {},
};

export { LocalStorage };
